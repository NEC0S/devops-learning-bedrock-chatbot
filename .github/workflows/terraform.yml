name: Terraform

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

env:
  AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
  AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
  AWS_REGION: eu-north-1
  TF_VAR_frontend_bucket_name: ${{ secrets.FRONTEND_BUCKET_NAME }}

jobs:
  terraform:
    name: Terraform Plan/Apply
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.7.0

      - name: Terraform Init
        run: terraform init

      - name: Terraform Format Check
        run: terraform fmt -check

      - name: Terraform Validate
        run: terraform validate

      - name: Terraform Plan
        run: terraform plan -input=false

      - name: Terraform Apply
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: terraform apply -auto-approve -input=false

      - name: Set up Python
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install script dependencies
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: pip install boto3

      - name: Seed knowledge base
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: python scripts/ingest_knowledge_base.py
        env:
          TABLE_NAME: simple-bedrock-chatbot-knowledge-base

      - name: Seed sample orders
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: python scripts/seed_orders.py
        env:
          TABLE_NAME: simple-bedrock-chatbot-orders
